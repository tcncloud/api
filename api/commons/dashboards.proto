syntax = "proto3";

package api.commons;

// DashboardReference containst he info needed to get a dashboard.
message DashboardReference {
  // The resource id of the dashboard.
  string resource_id = 1;
  // The title of the dashboard.
  string title = 2;
}
