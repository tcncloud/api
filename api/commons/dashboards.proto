syntax = "proto3";

package api.commons;

// Dashboard represents a dashboard.
message Dashboard {
  // The resource id of the dashboard.
  string id = 1;
  // The title of the dashboard.
  string title = 2;
}
